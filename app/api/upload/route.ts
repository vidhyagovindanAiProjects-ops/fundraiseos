import { NextResponse } from "next/server";
export async function POST(request:Request){const form=await request.formData();const file=form.get("file");if(!(file instanceof File))return NextResponse.json({error:"A file is required"},{status:400});const allowed=["application/pdf","application/vnd.openxmlformats-officedocument.wordprocessingml.document","text/plain"];if(!allowed.includes(file.type)&&!file.name.match(/\.(pdf|docx|txt)$/i))return NextResponse.json({error:"PDF, DOCX, and TXT only"},{status:415});return NextResponse.json({document:{name:file.name,size:file.size,status:"processed"},extraction:{people:["Elena Park","Marcus Chen"],organizations:["Northstar Family Office"],interests:["AI infrastructure"],concerns:["Fund II attribution"],actionItems:[{title:"Send data room access",due:"2026-06-23"}],summary:"LP expressed strong intent and requested diligence materials."}})}
