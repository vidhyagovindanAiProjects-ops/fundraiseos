"use client";
import { useState } from "react";
import { ArrowRight, Check, Sparkles } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
import "./login.css";
export default function LoginPage(){const [email,setEmail]=useState(""),[sent,setSent]=useState(false),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setError("");const supabase=createClient();if(!supabase){sessionStorage.setItem("fundraiseos_demo_user",email);window.location.href="/";return}const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${window.location.origin}/`}});if(error)setError(error.message);else setSent(true)}return <main className="login-page"><section className="login-brand"><div className="login-logo"><b>F</b> Fundraise<span>OS</span></div><div><label>AI-NATIVE RELATIONSHIP INTELLIGENCE</label><h1>Remember every LP conversation.<br/><em>Never miss the next best action.</em></h1><p>FundraiseOS turns meetings, notes, and introductions into a living fundraising memory.</p><ul><li><Check/>Every relationship, connected</li><li><Check/>Every commitment, remembered</li><li><Check/>Every next action, surfaced</li></ul></div><footer>Built for emerging fund managers.</footer></section><section className="login-form"><form onSubmit={submit}>{sent?<div className="login-sent"><Sparkles/><h2>Check your inbox</h2><p>We sent a secure sign-in link to <b>{email}</b>.</p></div>:<><small>WELCOME TO FUNDRAISEOS</small><h2>Your fundraising memory<br/>starts here.</h2><p>Sign in with your work email. No password required.</p><label>Email address</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@fund.com"/><button>Continue with email <ArrowRight/></button>{error&&<em>{error}</em>}<div className="demo-note"><Sparkles/> Without Supabase keys, any email opens the local demo.</div></>}</form></section></main>}
